# Revision 1 on the brief/plan

I had stated no backend, but as long as we can run a single `npm run` command that serves the frontend and any backend services, that is fine, and probably makes watching files easier.